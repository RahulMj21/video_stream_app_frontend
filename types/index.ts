export interface User {
  name: string;
  email: string;
  role: string;
}
export interface Video {
  videoTitle: string;
  videoDescription: string;
  creator: string;
  extension: string;
  videoId: string;
  published: Boolean;
}
